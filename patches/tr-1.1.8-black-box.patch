diff --git a/sicp-pocket.texi b/sicp-pocket.texi
index 5714513..9732425 100644
--- a/sicp-pocket.texi
+++ b/sicp-pocket.texi
@@ -2356,28 +2356,27 @@ x/y^2 + 2y
 @end quotation
 
 @node	1.1.8, 1.2, Chapter 1, 1.1
-@subsection Procedures as Black-Box Abstractions
-
-@code{Sqrt} is our first example of a process defined by a set of mutually
-defined procedures.  Notice that the definition of @code{sqrt-iter} is
-@newterm{recursive}; that is, the procedure is defined in terms of itself.  The
-idea of being able to define a procedure in terms of itself may be disturbing;
-it may seem unclear how such a ``circular'' definition could make sense at all,
-much less specify a well-defined process to be carried out by a computer.  This
-will be addressed more carefully in @ref{1.2}.  But first let's
-consider some other important points illustrated by the @code{sqrt} example.
-
-Observe that the problem of computing square roots breaks up naturally into a
-number of subproblems: how to tell whether a guess is good enough, how to
-improve a guess, and so on.  Each of these tasks is accomplished by a separate
-procedure.  The entire @code{sqrt} program can be viewed as a cluster of
-procedures (shown in @ref{Figure 1.2}) that mirrors the decomposition of the
-problem into subproblems.
+@subsection Процедуры как абстракции вида «чёрный ящик»
+
+@code{Sqrt} — наш первый пример процесса, определённого набором взаимно
+определённых процедур.  Заметьте, что определение @code{sqrt-iter}
+@newterm{рекурсивно}, то есть процедура определена через саму себя.  Мысль о том,
+что процедуру можно определить через саму себя, может смущать; может казаться
+неясным, как такое «круговое» определение вообще способно иметь смысл, не говоря уж
+о том, чтобы задавать вполне определённый процесс, выполняемый компьютером.  Это
+будет рассмотрено более тщательно в @ref{1.2}.  Но сначала рассмотрим некоторые
+другие важные моменты, проиллюстрированные примером @code{sqrt}.
+
+Заметьте, что задача вычисления квадратных корней естественно разбивается на ряд
+подзадач: как определить, достаточно ли хороша догадка, как улучшить догадку и так
+далее.  Каждая из этих задач решается отдельной процедурой.  Всю программу
+@code{sqrt} можно рассматривать как гроздь процедур (показанную на @ref{Figure 1.2}),
+отражающую разбиение задачи на подзадачи.
 
 @float
 @anchor{Figure 1.2}
 @ifinfo
-@strong{Figure 1.2:} Procedural decomposition of the @code{sqrt} program.
+@strong{Figure 1.2:} Процедурная декомпозиция программы @code{sqrt}.
 
 @example
                  sqrt
@@ -2391,34 +2390,32 @@ problem into subproblems.
 @end ifinfo
 @iftex
 @image{fig/chap1/Fig1.2,,38mm,,.std.svg}
-@caption{@strong{Figure 1.2:} Procedural decomposition of the @code{sqrt} program.}
+@caption{@strong{Figure 1.2:} Процедурная декомпозиция программы @code{sqrt}.}
 @end iftex
 @end float
 
 @noindent
-The importance of this decomposition strategy is not simply that one is
-dividing the program into parts.  After all, we could take any large program
-and divide it into parts---the first ten lines, the next ten lines, the next
-ten lines, and so on.  Rather, it is crucial that each procedure accomplishes
-an identifiable task that can be used as a module in defining other procedures.
-For example, when we define the @code{good-enough?} procedure in terms of
-@code{square}, we are able to regard the @code{square} procedure as a ``black
-box.''  We are not at that moment concerned with @emph{how} the procedure
-computes its result, only with the fact that it computes the square.  The
-details of how the square is computed can be suppressed, to be considered at a
-later time.  Indeed, as far as the @code{good-enough?} procedure is concerned,
-@code{square} is not quite a procedure but rather an abstraction of a
-procedure, a so-called @newterm{procedural abstraction}.  At this level of
-abstraction, any procedure that computes the square is equally good.
-
-Thus, considering only the values they return, the following two procedures for
-squaring a number should be indistinguishable.  Each takes a numerical argument
-and produces the square of that number as the value.@footnote{It is not even
-clear which of these procedures is a more efficient implementation.  This
-depends upon the hardware available.  There are machines for which the
-``obvious'' implementation is the less efficient one.  Consider a machine that
-has extensive tables of logarithms and antilogarithms stored in a very
-efficient manner.}
+Важность этой стратегии декомпозиции не просто в том, что программа делится на
+части.  В конце концов, любую большую программу можно поделить на части — первые
+десять строк, следующие десять строк, следующие десять строк и так далее.  Дело,
+скорее, в том, что критически важно, чтобы каждая процедура решала опознаваемую
+задачу, которую можно использовать как модуль при определении других процедур.
+Например, когда мы определяем процедуру @code{good-enough?} через @code{square}, мы
+можем рассматривать процедуру @code{square} как «чёрный ящик».  В этот момент нас не
+заботит, @emph{как} процедура вычисляет свой результат, — лишь тот факт, что она
+вычисляет квадрат.  Подробности того, как вычисляется квадрат, можно скрыть, чтобы
+рассмотреть их позднее.  В самом деле, с точки зрения процедуры @code{good-enough?}
+@code{square} — не вполне процедура, а скорее абстракция процедуры, так называемая
+@newterm{процедурная абстракция}.  На этом уровне абстракции любая процедура,
+вычисляющая квадрат, одинаково хороша.
+
+Таким образом, если рассматривать только возвращаемые ими значения, следующие две
+процедуры возведения числа в квадрат должны быть неразличимы.  Каждая принимает
+числовой аргумент и выдаёт как значение квадрат этого числа.@footnote{Даже неясно,
+какая из этих процедур — более эффективная реализация.  Это зависит от доступного
+оборудования.  Есть машины, для которых «очевидная» реализация оказывается менее
+эффективной.  Представьте машину, в которой обширные таблицы логарифмов и
+антилогарифмов хранятся очень эффективным образом.}
 
 @lisp
 (define (square x) (* x x))
@@ -2430,17 +2427,16 @@ efficient manner.}
 @end lisp
 
 @noindent
-So a procedure definition should be able to suppress detail.  The users of the
-procedure may not have written the procedure themselves, but may have obtained
-it from another programmer as a black box.  A user should not need to know how
-the procedure is implemented in order to use it.
+Итак, определение процедуры должно уметь скрывать детали.  Пользователи процедуры,
+возможно, не писали её сами, а получили от другого программиста как чёрный ящик.
+Пользователю не должно требоваться знать, как процедура реализована, чтобы ею
+пользоваться.
 
-@subsubheading Local names
+@subsubheading Локальные имена
 
-One detail of a procedure's implementation that should not matter to the user
-of the procedure is the implementer's choice of names for the procedure's
-formal parameters.  Thus, the following procedures should not be
-distinguishable:
+Одна деталь реализации процедуры, которая не должна иметь значения для пользователя
+процедуры, — это выбор реализатором имён для формальных параметров процедуры.
+Поэтому следующие процедуры должны быть неразличимы:
 
 @lisp
 (define (square x) (* x x))
@@ -2448,12 +2444,11 @@ distinguishable:
 @end lisp
 
 @noindent
-This principle---that the meaning of a procedure should be independent of the
-parameter names used by its author---seems on the surface to be self-evident,
-but its consequences are profound.  The simplest consequence is that the
-parameter names of a procedure must be local to the body of the procedure.  For
-example, we used @code{square} in the definition of @code{good-enough?} in our
-square-root procedure:
+Этот принцип — что смысл процедуры должен быть независим от имён параметров,
+выбранных её автором, — на поверхности кажется самоочевидным, но его следствия
+глубоки.  Простейшее следствие в том, что имена параметров процедуры должны быть
+локальными по отношению к телу процедуры.  Например, мы использовали @code{square}
+в определении @code{good-enough?} в нашей процедуре извлечения квадратного корня:
 
 @lisp
 (define (good-enough? guess x)
@@ -2461,56 +2456,53 @@ square-root procedure:
 @end lisp
 
 @noindent
-The intention of the author of @code{good-enough?} is to determine if the
-square of the first argument is within a given tolerance of the second
-argument.  We see that the author of @code{good-enough?} used the name
-@code{guess} to refer to the first argument and @code{x} to refer to the second
-argument.  The argument of @code{square} is @code{guess}.  If the author of
-@code{square} used @code{x} (as above) to refer to that argument, we see that
-the @code{x} in @code{good-enough?} must be a different @code{x} than the one
-in @code{square}.  Running the procedure @code{square} must not affect the
-value of @code{x} that is used by @code{good-enough?}, because that value of
-@code{x} may be needed by @code{good-enough?} after @code{square} is done
-computing.
-
-If the parameters were not local to the bodies of their respective procedures,
-then the parameter @code{x} in @code{square} could be confused with the
-parameter @code{x} in @code{good-enough?}, and the behavior of
-@code{good-enough?} would depend upon which version of @code{square} we used.
-Thus, @code{square} would not be the black box we desired.
-
-A formal parameter of a procedure has a very special role in the procedure
-definition, in that it doesn't matter what name the formal parameter has.  Such
-a name is called a @newterm{bound variable}, and we say that the procedure
-definition @newterm{binds} its formal parameters.  The meaning of a procedure
-definition is unchanged if a bound variable is consistently renamed throughout
-the definition.@footnote{The concept of consistent renaming is actually subtle
-and difficult to define formally.  Famous logicians have made embarrassing
-errors here.}  If a variable is not bound, we say that it is @newterm{free}.
-The set of expressions for which a binding defines a name is called the
-@newterm{scope} of that name.  In a procedure definition, the bound variables
-declared as the formal parameters of the procedure have the body of the
-procedure as their scope.
-
-In the definition of @code{good-enough?} above, @code{guess} and @code{x} are
-bound variables but @code{<}, @code{-}, @code{abs}, and @code{square} are free.
-The meaning of @code{good-enough?} should be independent of the names we choose
-for @code{guess} and @code{x} so long as they are distinct and different from
-@code{<}, @code{-}, @code{abs}, and @code{square}.  (If we renamed @code{guess}
-to @code{abs} we would have introduced a bug by @newterm{capturing} the
-variable @code{abs}.  It would have changed from free to bound.)  The meaning
-of @code{good-enough?} is not independent of the names of its free variables,
-however.  It surely depends upon the fact (external to this definition) that
-the symbol @code{abs} names a procedure for computing the absolute value of a
-number.  @code{Good-enough?} will compute a different function if we substitute
-@code{cos} for @code{abs} in its definition.
-
-@subsubheading Internal definitions and block structure
-
-We have one kind of name isolation available to us so far: The formal
-parameters of a procedure are local to the body of the procedure.  The
-square-root program illustrates another way in which we would like to control
-the use of names.  The existing program consists of separate procedures:
+Замысел автора @code{good-enough?} — определить, находится ли квадрат первого
+аргумента в пределах заданного допуска от второго аргумента.  Мы видим, что автор
+@code{good-enough?} использовал имя @code{guess} для обращения к первому аргументу
+и @code{x} — для обращения ко второму.  Аргумент @code{square} — это @code{guess}.
+Если автор @code{square} использовал @code{x} (как выше) для обращения к этому
+аргументу, то мы видим, что @code{x} в @code{good-enough?} должен быть иным
+@code{x}, нежели тот, что в @code{square}.  Выполнение процедуры @code{square} не
+должно затрагивать значение @code{x}, используемое @code{good-enough?}, потому что
+это значение @code{x} может понадобиться @code{good-enough?} уже после того, как
+@code{square} завершит вычисление.
+
+Если бы параметры не были локальными по отношению к телам соответствующих процедур,
+то параметр @code{x} в @code{square} можно было бы спутать с параметром @code{x} в
+@code{good-enough?}, и поведение @code{good-enough?} зависело бы от того, какую
+версию @code{square} мы использовали.  Тогда @code{square} не была бы тем чёрным
+ящиком, который нам нужен.
+
+Формальный параметр процедуры играет в определении процедуры совершенно особую роль:
+неважно, какое имя у формального параметра.  Такое имя называется @newterm{связанной
+переменной}, и мы говорим, что определение процедуры @newterm{связывает} свои
+формальные параметры.  Смысл определения процедуры не меняется, если связанную
+переменную согласованно переименовать по всему определению.@footnote{Понятие
+согласованного переименования на самом деле тонко и трудно поддаётся формальному
+определению.  Знаменитые логики допускали здесь конфузные ошибки.}  Если переменная
+не связана, мы говорим, что она @newterm{свободна}.  Множество выражений, для
+которых связывание определяет имя, называется @newterm{областью действия} этого
+имени.  В определении процедуры связанные переменные, объявленные как формальные
+параметры процедуры, имеют областью действия тело процедуры.
+
+В приведённом выше определении @code{good-enough?} переменные @code{guess} и
+@code{x} — связанные, а @code{<}, @code{-}, @code{abs} и @code{square} — свободные.
+Смысл @code{good-enough?} должен быть независим от имён, которые мы выбираем для
+@code{guess} и @code{x}, лишь бы они были различны и отличались от @code{<},
+@code{-}, @code{abs} и @code{square}.  (Если бы мы переименовали @code{guess} в
+@code{abs}, мы внесли бы баг, @newterm{захватив} переменную @code{abs}.  Она
+превратилась бы из свободной в связанную.)  Однако смысл @code{good-enough?} не
+независим от имён её свободных переменных.  Он, безусловно, зависит от (внешнего по
+отношению к этому определению) факта, что символ @code{abs} именует процедуру
+вычисления абсолютной величины числа.  @code{Good-enough?} будет вычислять другую
+функцию, если в её определении заменить @code{abs} на @code{cos}.
+
+@subsubheading Внутренние определения и блочная структура
+
+До сих пор нам доступен один вид изоляции имён: формальные параметры процедуры
+локальны по отношению к телу процедуры.  Программа извлечения квадратного корня
+иллюстрирует ещё один способ, которым нам хотелось бы управлять использованием имён.
+Существующая программа состоит из отдельных процедур:
 
 @lisp
 (define (sqrt x) 
@@ -2529,21 +2521,21 @@ the use of names.  The existing program consists of separate procedures:
 @end lisp
 
 @noindent
-The problem with this program is that the only procedure that is important to
-users of @code{sqrt} is @code{sqrt}.  The other procedures (@code{sqrt-iter},
-@code{good-enough?}, and @code{improve}) only clutter up their minds.  They may
-not define any other procedure called @code{good-enough?} as part of another
-program to work together with the square-root program, because @code{sqrt}
-needs it.  The problem is especially severe in the construction of large
-systems by many separate programmers.  For example, in the construction of a
-large library of numerical procedures, many numerical functions are computed as
-successive approximations and thus might have procedures named
-@code{good-enough?} and @code{improve} as auxiliary procedures.  We would like
-to localize the subprocedures, hiding them inside @code{sqrt} so that
-@code{sqrt} could coexist with other successive approximations, each having its
-own private @code{good-enough?} procedure.  To make this possible, we allow a
-procedure to have internal definitions that are local to that procedure.  For
-example, in the square-root problem we can write
+Проблема этой программы в том, что единственная процедура, важная для пользователей
+@code{sqrt}, — это @code{sqrt}.  Остальные процедуры (@code{sqrt-iter},
+@code{good-enough?} и @code{improve}) лишь засоряют их сознание.  Они не могут
+определить какую-либо другую процедуру с именем @code{good-enough?} в составе другой
+программы, работающей вместе с программой извлечения квадратного корня, потому что
+@code{sqrt} в ней нуждается.  Особенно остро проблема стоит при построении больших
+систем многими отдельными программистами.  Например, при построении большой
+библиотеки числовых процедур многие числовые функции вычисляются как
+последовательные приближения и потому могут иметь вспомогательные процедуры с
+именами @code{good-enough?} и @code{improve}.  Нам хотелось бы локализовать
+подпроцедуры, спрятав их внутри @code{sqrt}, чтобы @code{sqrt} могла сосуществовать
+с другими последовательными приближениями, у каждого из которых своя собственная
+процедура @code{good-enough?}.  Чтобы сделать это возможным, мы позволяем процедуре
+иметь внутренние определения, локальные для этой процедуры.  Например, в задаче
+извлечения квадратного корня мы можем написать
 
 @lisp
 (define (sqrt x)
@@ -2559,21 +2551,21 @@ example, in the square-root problem we can write
 @end lisp
 
 @noindent
-Such nesting of definitions, called @newterm{block structure}, is basically the
-right solution to the simplest name-packaging problem.  But there is a better
-idea lurking here.  In addition to internalizing the definitions of the
-auxiliary procedures, we can simplify them.  Since @code{x} is bound in the
-definition of @code{sqrt}, the procedures @code{good-enough?}, @code{improve},
-and @code{sqrt-iter}, which are defined internally to @code{sqrt}, are in the
-scope of @code{x}.  Thus, it is not necessary to pass @code{x} explicitly to
-each of these procedures.  Instead, we allow @code{x} to be a free variable in
-the internal definitions, as shown below. Then @code{x} gets its value from the
-argument with which the enclosing procedure @code{sqrt} is called.  This
-discipline is called @newterm{lexical scoping}.@footnote{Lexical scoping dictates 
-that free variables in a procedure are taken to refer to bindings made by enclosing 
-procedure definitions; that is, they are looked up in the environment in which the
-procedure was defined. We will see how this works in detail in chapter 3 when we study 
-environments and the detailed behavior of the interpreter.@anchor{Footnote 28}}
+Такое вложение определений, называемое @newterm{блочной структурой}, по сути и есть
+правильное решение простейшей задачи упаковки имён.  Но здесь притаилась идея
+получше.  Помимо того, что мы помещаем определения вспомогательных процедур внутрь,
+мы можем их упростить.  Поскольку @code{x} связан в определении @code{sqrt},
+процедуры @code{good-enough?}, @code{improve} и @code{sqrt-iter}, определённые
+внутри @code{sqrt}, находятся в области действия @code{x}.  Поэтому необязательно
+явно передавать @code{x} каждой из этих процедур.  Вместо этого мы позволяем
+@code{x} быть свободной переменной во внутренних определениях, как показано ниже.
+Тогда @code{x} получает своё значение из аргумента, с которым вызывается
+объемлющая процедура @code{sqrt}.  Эта дисциплина называется @newterm{лексической
+областью видимости}.@footnote{Лексическая область видимости предписывает, что
+свободные переменные в процедуре считаются ссылающимися на связывания, сделанные
+объемлющими определениями процедур; то есть они отыскиваются в окружении, в котором
+процедура была определена.  Как это работает в подробностях, мы увидим в главе 3,
+когда будем изучать окружения и детальное поведение интерпретатора.@anchor{Footnote 28}}
 
 @lisp
 (define (sqrt x)
@@ -2589,14 +2581,13 @@ environments and the detailed behavior of the interpreter.@anchor{Footnote 28}}
 @end lisp
 
 @noindent
-We will use block structure extensively to help us break up large
-programs into tractable pieces.@footnote{Embedded definitions must come
-first in a procedure body. The management is not responsible for the
-consequences of running programs that intertwine definition and use.}
-The idea of block structure originated with the programming language
-Algol 60. It appears in most advanced programming languages and is an
-important tool for helping to organize the construction of large
-programs.
+Мы будем широко пользоваться блочной структурой, чтобы разбивать большие программы
+на удобоваримые куски.@footnote{Встроенные определения должны идти в начале тела
+процедуры.  Руководство не несёт ответственности за последствия запуска программ, в
+которых определение и использование переплетены.}
+Идея блочной структуры зародилась в языке программирования Algol 60.  Она
+встречается в большинстве развитых языков программирования и является важным
+инструментом, помогающим организовать построение больших программ.
 
 @node	1.2, 1.3, 1.1, Chapter 1
 @section Procedures and the Processes They Generate
