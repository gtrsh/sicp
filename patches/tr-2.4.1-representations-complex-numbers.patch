diff --git a/sicp-pocket.texi b/sicp-pocket.texi
index 7d3440d..9581b62 100644
--- a/sicp-pocket.texi
+++ b/sicp-pocket.texi
@@ -11081,29 +11081,28 @@ Sha boom
 @end menu
 
 @node	2.4.1, 2.4.2, 2.4, 2.4
-@subsection Representations for Complex Numbers
-
-We will develop a system that performs arithmetic operations on complex numbers
-as a simple but unrealistic example of a program that uses generic operations.
-We begin by discussing two plausible representations for complex numbers as
-ordered pairs: rectangular form (real part and imaginary part) and polar form
-(magnitude and angle).@footnote{In actual computational systems, rectangular
-form is preferable to polar form most of the time because of roundoff errors in
-conversion between rectangular and polar form.  This is why the complex-number
-example is unrealistic.  Nevertheless, it provides a clear illustration of the
-design of a system using generic operations and a good introduction to the more
-substantial systems to be developed later in this chapter.}  
-Section @ref{2.4.2} will show how both representations can be made to coexist in a
-single system through the use of type tags and generic operations.
-
-Like rational numbers, complex numbers are naturally represented as ordered
-pairs.  The set of complex numbers can be thought of as a two-dimensional space
-with two orthogonal axes, the ``real'' axis and the ``imaginary'' axis. (See
-@ref{Figure 2.20}.)  From this point of view, the complex number 
-@math{{z = x + iy}} (where @math{{i^{\kern0.1em 2} = \text{−1}}}) can be thought of as the point in the plane
-whose real coordinate is @math{x} and whose imaginary coordinate is @math{y}.
-Addition of complex numbers reduces in this representation to addition of
-coordinates:
+@subsection Представления для комплексных чисел
+
+Мы разработаем систему, которая выполняет арифметические операции над комплексными
+числами, как простой, но нереалистичный пример программы, использующей обобщённые
+операции.  Мы начнём с обсуждения двух правдоподобных представлений комплексных чисел как
+упорядоченных пар: прямоугольной формы (действительная часть и мнимая часть) и полярной
+формы (модуль и угол).@footnote{В реальных вычислительных системах прямоугольная форма
+предпочтительнее полярной большую часть времени из-за ошибок округления при преобразовании
+между прямоугольной и полярной формами.  Вот почему пример с комплексными числами
+нереалистичен.  Тем не менее он даёт ясную иллюстрацию проектирования системы, использующей
+обобщённые операции, и хорошее введение в более существенные системы, которые будут
+разработаны позднее в этой главе.}  Раздел @ref{2.4.2} покажет, как оба представления можно
+заставить сосуществовать в одной системе через использование тегов типа и обобщённых
+операций.
+
+Как и рациональные числа, комплексные числа естественно представляются как упорядоченные
+пары.  Множество комплексных чисел можно мыслить как двумерное пространство с двумя
+ортогональными осями — «действительной» осью и «мнимой» осью.  (См. @ref{Figure 2.20}.)
+С этой точки зрения комплексное число @math{{z = x + iy}} (где @math{{i^{\kern0.1em 2} = \text{−1}}})
+можно мыслить как точку на плоскости, чья действительная координата — @math{x}, а мнимая
+координата — @math{y}.  Сложение комплексных чисел сводится в этом представлении к сложению
+координат:
 @ifinfo
 
 @example
@@ -11127,10 +11126,10 @@ Imaginary-part(z_1 + z_2) = Imaginary-part(z_1) + Imaginary-part(z_2)
 @float
 @anchor{Figure 2.20}
 @ifinfo
-@strong{Figure 2.20:} Complex numbers as points in the plane.
+@strong{Figure 2.20:} Комплексные числа как точки на плоскости.
 
 @example
- Imaginary
+ Мнимая
     ^
     |
   y |.........................* z = x + ?y = r e^(?A)
@@ -11140,22 +11139,22 @@ Imaginary-part(z_1 + z_2) = Imaginary-part(z_1) + Imaginary-part(z_2)
     |        __--             .
     |    __-- \               .
     |__--    A |              .
-----+----------+-------------------> Real
+----+----------+-------------------> Действительная
                               x
 @end example
 @end ifinfo
 @iftex
 @image{fig/chap2/Fig2.20,147mm,,,.std.svg}
-@caption{@strong{Figure 2.20:} Complex numbers as points in the plane.}
+@caption{@strong{Figure 2.20:} Комплексные числа как точки на плоскости.}
 @end iftex
 @end float
 
 @noindent
-When multiplying complex numbers, it is more natural to think in terms of
-representing a complex number in polar form, as a magnitude and an angle (@math{r}
-and @math{A} in @ref{Figure 2.20}).  The product of two complex numbers is the
-vector obtained by stretching one complex number by the length of the other and
-then rotating it through the angle of the other:
+При умножении комплексных чисел более естественно мыслить в терминах представления
+комплексного числа в полярной форме, как модуля и угла (@math{r} и @math{A} на
+@ref{Figure 2.20}).  Произведение двух комплексных чисел — это вектор, полученный
+растяжением одного комплексного числа на длину другого и затем поворотом его на угол
+другого:
 @ifinfo
 
 @example
@@ -11177,25 +11176,23 @@ Angle(z_1 * z_2) = Angle(z_1) + Angle(z_2)
 @end tex
 
 @noindent
-Thus, there are two different representations for complex numbers, which are
-appropriate for different operations.  Yet, from the viewpoint of someone
-writing a program that uses complex numbers, the principle of data abstraction
-suggests that all the operations for manipulating complex numbers should be
-available regardless of which representation is used by the computer.  For
-example, it is often useful to be able to find the magnitude of a complex
-number that is specified by rectangular coordinates.  Similarly, it is often
-useful to be able to determine the real part of a complex number that is
-specified by polar coordinates.
+Таким образом, есть два различных представления для комплексных чисел, подходящие для
+разных операций.  И всё же, с точки зрения того, кто пишет программу, использующую
+комплексные числа, принцип абстракции данных подсказывает, что все операции для
+манипулирования комплексными числами должны быть доступны независимо от того, какое
+представление используется компьютером.  Например, часто полезно уметь найти модуль
+комплексного числа, заданного прямоугольными координатами.  Аналогично, часто полезно уметь
+определить действительную часть комплексного числа, заданного полярными координатами.
 
-To design such a system, we can follow the same data-abstraction strategy we
-followed in designing the rational-number package in @ref{2.1.1}.
-Assume that the operations on complex numbers are implemented in terms of four
-selectors: @code{real-part}, @code{imag-part}, @code{magnitude}, and
-@code{angle}.  Also assume that we have two procedures for constructing complex
-numbers: @code{make-from-real-imag} returns a complex number with specified
-real and imaginary parts, and @code{make-from-mag-ang} returns a complex number
-with specified magnitude and angle.  These procedures have the property that,
-for any complex number @code{z}, both
+Чтобы спроектировать такую систему, мы можем следовать той же стратегии абстракции данных,
+которой мы следовали при проектировании пакета рациональных чисел в @ref{2.1.1}.
+Предположим, что операции над комплексными числами реализованы в терминах четырёх
+селекторов: @code{real-part}, @code{imag-part}, @code{magnitude} и @code{angle}.  Также
+предположим, что у нас есть две процедуры для построения комплексных чисел:
+@code{make-from-real-imag} возвращает комплексное число с заданными действительной и мнимой
+частями, а @code{make-from-mag-ang} возвращает комплексное число с заданными модулем и
+углом.  Эти процедуры обладают тем свойством, что для любого комплексного числа @code{z}
+оба
 
 @lisp
 (make-from-real-imag (real-part z) 
@@ -11203,7 +11200,7 @@ for any complex number @code{z}, both
 @end lisp
 
 @noindent
-and
+и
 
 @lisp
 (make-from-mag-ang (magnitude z) 
@@ -11211,14 +11208,13 @@ and
 @end lisp
 
 @noindent
-produce complex numbers that are equal to @code{z}.
+производят комплексные числа, равные @code{z}.
 
-Using these constructors and selectors, we can implement arithmetic on complex
-numbers using the ``abstract data'' specified by the constructors and
-selectors, just as we did for rational numbers in @ref{2.1.1}.  As
-shown in the formulas above, we can add and subtract complex numbers in terms
-of real and imaginary parts while multiplying and dividing complex numbers in
-terms of magnitudes and angles:
+Используя эти конструкторы и селекторы, мы можем реализовать арифметику над комплексными
+числами, используя «абстрактные данные», заданные конструкторами и селекторами, точно так
+же, как мы делали для рациональных чисел в @ref{2.1.1}.  Как показано в формулах выше, мы
+можем складывать и вычитать комплексные числа в терминах действительных и мнимых частей,
+а умножать и делить комплексные числа в терминах модулей и углов:
 
 @lisp
 (define (add-complex z1 z2)
@@ -11243,21 +11239,19 @@ terms of magnitudes and angles:
 @end lisp
 
 @noindent
-To complete the complex-number package, we must choose a representation and we
-must implement the constructors and selectors in terms of primitive numbers and
-primitive list structure.  There are two obvious ways to do this: We can
-represent a complex number in ``rectangular form'' as a pair (real part,
-imaginary part) or in ``polar form'' as a pair (magnitude, angle).  Which shall
-we choose?
+Чтобы завершить пакет комплексных чисел, мы должны выбрать представление и должны
+реализовать конструкторы и селекторы в терминах примитивных чисел и примитивной списочной
+структуры.  Есть два очевидных способа сделать это: мы можем представить комплексное число
+в «прямоугольной форме» как пару (действительная часть, мнимая часть) или в «полярной
+форме» как пару (модуль, угол).  Что мы выберем?
 
-In order to make the different choices concrete, imagine that there are two
-programmers, Ben Bitdiddle and Alyssa P. Hacker, who are independently
-designing representations for the complex-number system.  Ben chooses to
-represent complex numbers in rectangular form.  With this choice, selecting the
-real and imaginary parts of a complex number is straightforward, as is
-constructing a complex number with given real and imaginary parts.  To find the
-magnitude and the angle, or to construct a complex number with a given
-magnitude and angle, he uses the trigonometric relations
+Чтобы сделать различные выборы конкретными, вообразим, что есть два программиста, Бен
+Битдидл и Алисса П. Хакер, которые независимо проектируют представления для системы
+комплексных чисел.  Бен выбирает представлять комплексные числа в прямоугольной форме.  С
+этим выбором выбор действительной и мнимой частей комплексного числа прямолинеен, как и
+построение комплексного числа с данными действительной и мнимой частями.  Чтобы найти
+модуль и угол или построить комплексное число с данными модулем и углом, он использует
+тригонометрические соотношения
 
 @ifinfo
 
@@ -11281,12 +11275,11 @@ y = r sin A     A = arctan(y,x)
 @end tex
 
 @noindent
-which relate the real and imaginary parts @math{{(x, y)}} to the magnitude and
-the angle @math{{(r, A)}}.@footnote{The arctangent function referred to here,
-computed by Scheme's @code{atan} procedure, is defined so as to take two
-arguments @math{y} and @math{x} and to return the angle whose tangent is @math{{y / x}}.
-The signs of the arguments determine the quadrant of the angle.}  Ben's
-representation is therefore given by the following selectors and constructors:
+которые связывают действительную и мнимую части @math{{(x, y)}} с модулем и углом
+@math{{(r, A)}}.@footnote{Функция арктангенса, упоминаемая здесь, вычисляемая процедурой
+@code{atan} в Scheme, определена так, чтобы принимать два аргумента @math{y} и @math{x} и
+возвращать угол, тангенс которого равен @math{{y / x}}.  Знаки аргументов определяют
+квадрант угла.}  Представление Бена поэтому даётся следующими селекторами и конструкторами:
 
 @lisp
 (define (real-part z) (car z))
@@ -11307,10 +11300,9 @@ representation is therefore given by the following selectors and constructors:
 @end lisp
 
 @noindent
-Alyssa, in contrast, chooses to represent complex numbers in polar form.  For
-her, selecting the magnitude and angle is straightforward, but she has to use
-the trigonometric relations to obtain the real and imaginary parts.  Alyssa's
-representation is:
+Алисса, напротив, выбирает представлять комплексные числа в полярной форме.  Для неё выбор
+модуля и угла прямолинеен, но ей приходится использовать тригонометрические соотношения,
+чтобы получить действительную и мнимую части.  Представление Алиссы:
 
 @lisp
 (define (real-part z)
@@ -11331,10 +11323,9 @@ representation is:
 @end lisp
 
 @noindent
-The discipline of data abstraction ensures that the same implementation of
-@code{add-complex}, @code{sub-complex}, @code{mul-complex}, and
-@code{div-complex} will work with either Ben's representation or Alyssa's
-representation.
+Дисциплина абстракции данных обеспечивает, что одна и та же реализация @code{add-complex},
+@code{sub-complex}, @code{mul-complex} и @code{div-complex} будет работать как с
+представлением Бена, так и с представлением Алиссы.
 
 @node	2.4.2, 2.4.3, 2.4.1, 2.4
 @subsection Tagged data
