diff --git a/sicp-pocket.texi b/sicp-pocket.texi
index dd8950f..4a601c9 100644
--- a/sicp-pocket.texi
+++ b/sicp-pocket.texi
@@ -1796,14 +1796,14 @@ Lisp использует вычисление в аппликативном п
 Scheme с нормальным порядком.}
 
 @node	1.1.6, 1.1.7, 1.1.5, 1.1
-@subsection Conditional Expressions and Predicates
-
-The expressive power of the class of procedures that we can define at this
-point is very limited, because we have no way to make tests and to perform
-different operations depending on the result of a test.  For instance, we
-cannot define a procedure that computes the absolute value of a number by
-testing whether the number is positive, negative, or zero and taking different
-actions in the different cases according to the rule
+@subsection Условные выражения и предикаты
+
+Выразительная сила класса процедур, которые мы умеем определять на данный момент,
+весьма ограничена, потому что у нас нет способа делать проверки и выполнять разные
+операции в зависимости от результата проверки.  Например, мы не можем определить
+процедуру, которая вычисляет абсолютную величину числа, проверяя, положительно
+число, отрицательно или равно нулю, и совершая разные действия в разных случаях по
+правилу
 @ifinfo
 
 @example
@@ -1830,9 +1830,9 @@ actions in the different cases according to the rule
 \]
 
 @end tex
-This construct is called a @newterm{case analysis}, and there is a special form
-in Lisp for notating such a case analysis.  It is called @code{cond} (which
-stands for ``conditional''), and it is used as follows:
+Эта конструкция называется @newterm{разбором случаев}, и в Lisp есть специальная
+форма для записи такого разбора случаев.  Она называется @code{cond} (от
+«conditional» — «условный») и используется так:
 
 @lisp
 (define (abs x)
@@ -1842,7 +1842,7 @@ stands for ``conditional''), and it is used as follows:
 @end lisp
 
 @noindent
-The general form of a conditional expression is
+Общая форма условного выражения такова:
 
 @lisp
 (cond (⟨@var{p₁}⟩ ⟨@var{e₁}⟩)
@@ -1852,43 +1852,42 @@ The general form of a conditional expression is
 @end lisp
 
 @noindent
-consisting of the symbol @code{cond} followed by parenthesized pairs of
-expressions
+она состоит из символа @code{cond}, за которым следуют заключённые в скобки пары
+выражений
 
 @lisp
 (⟨@var{p}⟩ ⟨@var{e}⟩)
 @end lisp
 
 @noindent
-called @newterm{clauses}. The first expression in each pair is a
-@newterm{predicate}---that is, an expression whose value is interpreted as
-either true or false.@footnote{``Interpreted as either true or false'' means
-this: In Scheme, there are two distinguished values that are denoted by the
-constants @code{#t} and @code{#f}.  When the interpreter checks a predicate's
-value, it interprets @code{#f} as false.  Any other value is treated as true.
-(Thus, providing @code{#t} is logically unnecessary, but it is convenient.)  In
-this book we will use names @code{true} and @code{false}, which are associated
-with the values @code{#t} and @code{#f} respectively.}
+называемые @newterm{предложениями}.  Первое выражение в каждой паре — это
+@newterm{предикат}, то есть выражение, значение которого истолковывается как истина
+либо ложь.@footnote{«Истолковывается как истина либо ложь» означает следующее.  В
+Scheme есть два выделенных значения, обозначаемых константами @code{#t} и
+@code{#f}.  Проверяя значение предиката, интерпретатор истолковывает @code{#f} как
+ложь.  Любое другое значение трактуется как истина.  (Так что наличие @code{#t}
+логически излишне, но удобно.)  В этой книге мы будем использовать имена
+@code{true} и @code{false}, связанные со значениями @code{#t} и @code{#f}
+соответственно.}
 
-Conditional expressions are evaluated as follows.  The predicate @math{{⟨p_1⟩}} is
-evaluated first.  If its value is false, then @math{{⟨p_2⟩}} is evaluated.  If
-@math{{⟨p_2⟩}}'s value is also false, then @math{{⟨p_3⟩}} is evaluated.  This process
-continues until a predicate is found whose value is true, in which case the
-interpreter returns the value of the corresponding @newterm{consequent expression} 
-@math{{⟨e⟩}} of the clause as the value of the conditional expression.
-If none of the @math{{⟨p⟩}}'s is found to be true, the value of the @code{cond} is
-undefined.
+Условные выражения вычисляются так.  Сначала вычисляется предикат @math{{⟨p_1⟩}}.
+Если его значение ложно, то вычисляется @math{{⟨p_2⟩}}.  Если значение
+@math{{⟨p_2⟩}} тоже ложно, вычисляется @math{{⟨p_3⟩}}.  Этот процесс продолжается,
+пока не найдётся предикат, значение которого истинно; в этом случае интерпретатор
+возвращает как значение условного выражения значение соответствующего
+@newterm{выражения-следствия} @math{{⟨e⟩}} этого предложения.  Если ни один из
+@math{{⟨p⟩}} не оказался истинным, значение @code{cond} не определено.
 
-The word @newterm{predicate} is used for procedures that return true or false,
-as well as for expressions that evaluate to true or false.  The absolute-value
-procedure @code{abs} makes use of the primitive predicates @code{>}, @code{<},
-and @code{=}.@footnote{@code{Abs} also uses the ``minus'' operator @code{-},
-which, when used with a single operand, as in @code{(- x)}, indicates
-negation.} These take two numbers as arguments and test whether the first
-number is, respectively, greater than, less than, or equal to the second
-number, returning true or false accordingly.
+Слово @newterm{предикат} употребляется как для процедур, возвращающих истину или
+ложь, так и для выражений, вычисляющихся в истину или ложь.  Процедура абсолютной
+величины @code{abs} использует примитивные предикаты @code{>}, @code{<} и
+@code{=}.@footnote{@code{Abs} использует также оператор «минус» @code{-}, который,
+будучи применён к одному операнду, как в @code{(- x)}, обозначает отрицание.} Они
+принимают два числа как аргументы и проверяют, является ли первое число
+соответственно больше, меньше или равно второму, возвращая истину или ложь сообразно
+этому.
 
-Another way to write the absolute-value procedure is
+Другой способ записать процедуру абсолютной величины таков:
 
 @lisp
 (define (abs x)
@@ -1897,15 +1896,14 @@ Another way to write the absolute-value procedure is
 @end lisp
 
 @noindent
-which could be expressed in English as ``If @math{x} is less than zero return 
-@math{{-x}}; otherwise return @math{x}.''  @code{Else} is a special symbol that can be
-used in place of the @math{{⟨p⟩}} in the final clause of a @code{cond}.  This
-causes the @code{cond} to return as its value the value of the corresponding
-@math{{⟨e⟩}} whenever all previous clauses have been bypassed.  In fact, any
-expression that always evaluates to a true value could be used as the @math{{⟨p⟩}}
-here.
+что по-русски можно выразить так: «Если @math{x} меньше нуля, верни @math{{-x}};
+иначе верни @math{x}».  @code{Else} — особый символ, который можно использовать
+вместо @math{{⟨p⟩}} в последнем предложении @code{cond}.  Это заставляет @code{cond}
+вернуть как своё значение значение соответствующего @math{{⟨e⟩}} всякий раз, когда
+все предыдущие предложения были пропущены.  По сути, на месте @math{{⟨p⟩}} здесь
+можно использовать любое выражение, которое всегда вычисляется в истинное значение.
 
-Here is yet another way to write the absolute-value procedure:
+Вот ещё один способ записать процедуру абсолютной величины:
 
 @lisp
 (define (abs x)
@@ -1915,74 +1913,74 @@ Here is yet another way to write the absolute-value procedure:
 @end lisp
 
 @noindent
-This uses the special form @code{if}, a restricted type of conditional that can
-be used when there are precisely two cases in the case analysis.  The general
-form of an @code{if} expression is
+Здесь используется специальная форма @code{if} — ограниченный вид условного
+выражения, применимый, когда в разборе случаев ровно два случая.  Общая форма
+выражения @code{if} такова:
 
 @lisp
-(if ⟨@var{predicate}⟩ ⟨@var{consequent}⟩ ⟨@var{alternative}⟩)
+(if ⟨@var{предикат}⟩ ⟨@var{следствие}⟩ ⟨@var{альтернатива}⟩)
 @end lisp
 
 @noindent
-To evaluate an @code{if} expression, the interpreter starts by evaluating the
-@code{⟨}@var{predicate}@code{⟩} part of the expression.  If the @code{⟨}@var{predicate}@code{⟩} evaluates
-to a true value, the interpreter then evaluates the @code{⟨}@var{consequent}@code{⟩} and
-returns its value.  Otherwise it evaluates the @code{⟨}@var{alternative}@code{⟩} and returns
-its value.@footnote{A minor difference between @code{if} and @code{cond} is
-that the @code{⟨}@var{e}@code{⟩} part of each @code{cond} clause may be a sequence of
-expressions.  If the corresponding @code{⟨}@var{p}@code{⟩} is found to be true, the
-expressions @code{⟨}@var{e}@code{⟩} are evaluated in sequence and the value of the final
-expression in the sequence is returned as the value of the @code{cond}.  In an
-@code{if} expression, however, the @code{⟨}@var{consequent}@code{⟩} and @code{⟨}@var{alternative}@code{⟩}
-must be single expressions.}
+Чтобы вычислить выражение @code{if}, интерпретатор начинает с вычисления части
+@code{⟨}@var{предикат}@code{⟩} выражения.  Если @code{⟨}@var{предикат}@code{⟩}
+вычисляется в истинное значение, интерпретатор затем вычисляет
+@code{⟨}@var{следствие}@code{⟩} и возвращает его значение.  Иначе он вычисляет
+@code{⟨}@var{альтернатива}@code{⟩} и возвращает её значение.@footnote{Небольшое
+различие между @code{if} и @code{cond} в том, что часть @code{⟨}@var{e}@code{⟩}
+каждого предложения @code{cond} может быть последовательностью выражений.  Если
+соответствующий @code{⟨}@var{p}@code{⟩} оказывается истинным, выражения
+@code{⟨}@var{e}@code{⟩} вычисляются по очереди, и значение последнего выражения
+последовательности возвращается как значение @code{cond}.  В выражении же @code{if}
+части @code{⟨}@var{следствие}@code{⟩} и @code{⟨}@var{альтернатива}@code{⟩} должны
+быть одиночными выражениями.}
 
-In addition to primitive predicates such as @code{<}, @code{=}, and @code{>},
-there are logical composition operations, which enable us to construct compound
-predicates.  The three most frequently used are these:
+Помимо примитивных предикатов вроде @code{<}, @code{=} и @code{>}, есть операции
+логической композиции, позволяющие строить составные предикаты.  Три наиболее часто
+используемые таковы:
 
 @itemize @bullet
 
 @item
 @code{(and ⟨@var{e₁}⟩ @r{…} ⟨@var{eₙ}⟩)}
 
-The interpreter evaluates the expressions @code{⟨}@var{e}@code{⟩} one at a time, in
-left-to-right order.  If any @code{⟨}@var{e}@code{⟩} evaluates to false, the value of the
-@code{and} expression is false, and the rest of the @code{⟨}@var{e}@code{⟩}'s are not
-evaluated.  If all @code{⟨}@var{e}@code{⟩}'s evaluate to true values, the value of the
-@code{and} expression is the value of the last one.
+Интерпретатор вычисляет выражения @code{⟨}@var{e}@code{⟩} по одному, слева направо.
+Если какое-либо @code{⟨}@var{e}@code{⟩} вычисляется в ложь, значением выражения
+@code{and} является ложь, и остальные @code{⟨}@var{e}@code{⟩} не вычисляются.  Если
+все @code{⟨}@var{e}@code{⟩} вычисляются в истинные значения, значением выражения
+@code{and} является значение последнего из них.
 
 @item
 @code{(or ⟨@var{e₁}⟩ @r{…} ⟨@var{eₙ}⟩)}
 
-The interpreter evaluates the expressions @code{⟨}@var{e}@code{⟩} one at a time, in
-left-to-right order.  If any @code{⟨}@var{e}@code{⟩} evaluates to a true value, that value is
-returned as the value of the @code{or} expression, and the rest of the
-@code{⟨}@var{e}@code{⟩}'s are not evaluated.  If all @code{⟨}@var{e}@code{⟩}'s evaluate to false, the value
-of the @code{or} expression is false.
+Интерпретатор вычисляет выражения @code{⟨}@var{e}@code{⟩} по одному, слева направо.
+Если какое-либо @code{⟨}@var{e}@code{⟩} вычисляется в истинное значение, это
+значение возвращается как значение выражения @code{or}, и остальные
+@code{⟨}@var{e}@code{⟩} не вычисляются.  Если все @code{⟨}@var{e}@code{⟩} вычисляются
+в ложь, значением выражения @code{or} является ложь.
 
 @item
 @code{(not ⟨@var{e}⟩)}
 
-The value of a @code{not} expression is true when the expression @code{⟨}@var{e}@code{⟩}
-evaluates to false, and false otherwise.
+Значение выражения @code{not} истинно, когда выражение @code{⟨}@var{e}@code{⟩}
+вычисляется в ложь, и ложно в противном случае.
 
 @end itemize
 
 @noindent
-Notice that @code{and} and @code{or} are special forms, not procedures, because
-the subexpressions are not necessarily all evaluated.  @code{Not} is an
-ordinary procedure.
+Заметьте, что @code{and} и @code{or} — специальные формы, а не процедуры, потому что
+их подвыражения не обязательно все вычисляются.  @code{Not} — обычная процедура.
 
-As an example of how these are used, the condition that a number @math{x} be in
-the range @math{5 < x < 10} may be expressed as
+В качестве примера их использования: условие, что число @math{x} лежит в диапазоне
+@math{5 < x < 10}, можно выразить как
 
 @lisp
 (and (> x 5) (< x 10))
 @end lisp
 
 @noindent
-As another example, we can define a predicate to test whether one number is
-greater than or equal to another as 
+В качестве другого примера определим предикат, проверяющий, что одно число больше
+либо равно другому:
 
 @lisp
 (define (>= x y) 
@@ -1990,7 +1988,7 @@ greater than or equal to another as
 @end lisp
 
 @noindent
-or alternatively as 
+или, как вариант, так:
 
 @lisp
 (define (>= x y) 
@@ -1998,10 +1996,9 @@ or alternatively as
 @end lisp
 
 @quotation
-@strong{@anchor{Exercise 1.1}Exercise 1.1:} Below is a sequence of expressions.
-What is the result printed by the interpreter in response to each expression?
-Assume that the sequence is to be evaluated in the order in which it is
-presented.
+@strong{@anchor{Exercise 1.1}Exercise 1.1:} Ниже приведена последовательность
+выражений.  Какой результат напечатает интерпретатор в ответ на каждое выражение?
+Считайте, что последовательность вычисляется в том порядке, в каком она приведена.
 
 @lisp
 10
@@ -2028,8 +2025,8 @@ presented.
 @end quotation
 
 @quotation
-@strong{@anchor{Exercise 1.2}Exercise 1.2:} Translate the following expression
-into prefix form:
+@strong{@anchor{Exercise 1.2}Exercise 1.2:} Переведите следующее выражение в
+префиксную форму:
 @ifinfo
 
 @example
@@ -2050,15 +2047,14 @@ into prefix form:
 @end quotation
 
 @quotation
-@strong{@anchor{Exercise 1.3}Exercise 1.3:} Define a procedure that takes three
-numbers as arguments and returns the sum of the squares of the two larger
-numbers.
+@strong{@anchor{Exercise 1.3}Exercise 1.3:} Определите процедуру, которая принимает
+три числа как аргументы и возвращает сумму квадратов двух бо́льших из них.
 @end quotation
 
 @quotation
-@strong{@anchor{Exercise 1.4}Exercise 1.4:} Observe that our model of
-evaluation allows for combinations whose operators are compound expressions.
-Use this observation to describe the behavior of the following procedure:
+@strong{@anchor{Exercise 1.4}Exercise 1.4:} Заметьте, что наша модель вычисления
+допускает комбинации, операторы которых — составные выражения.  Используйте это
+наблюдение, чтобы описать поведение следующей процедуры:
 
 @lisp
 (define (a-plus-abs-b a b)
@@ -2067,10 +2063,9 @@ Use this observation to describe the behavior of the following procedure:
 @end quotation
 
 @quotation
-@strong{@anchor{Exercise 1.5}Exercise 1.5:} Ben Bitdiddle has invented a test
-to determine whether the interpreter he is faced with is using
-applicative-order evaluation or normal-order evaluation.  He defines the
-following two procedures:
+@strong{@anchor{Exercise 1.5}Exercise 1.5:} Бен Битдидл изобрёл проверку, чтобы
+определить, использует ли интерпретатор, с которым он имеет дело, вычисление в
+аппликативном порядке или в нормальном.  Он определяет следующие две процедуры:
 
 @lisp
 (define (p) (p))
@@ -2081,19 +2076,19 @@ following two procedures:
       y))
 @end lisp
 
-Then he evaluates the expression
+Затем он вычисляет выражение
 
 @lisp
 (test 0 (p))
 @end lisp
 
-What behavior will Ben observe with an interpreter that uses applicative-order
-evaluation?  What behavior will he observe with an interpreter that uses
-normal-order evaluation?  Explain your answer.  (Assume that the evaluation
-rule for the special form @code{if} is the same whether the interpreter is
-using normal or applicative order: The predicate expression is evaluated first,
-and the result determines whether to evaluate the consequent or the alternative
-expression.)
+Какое поведение Бен будет наблюдать с интерпретатором, использующим вычисление в
+аппликативном порядке?  Какое поведение он будет наблюдать с интерпретатором,
+использующим вычисление в нормальном порядке?  Обоснуйте ответ.  (Считайте, что
+правило вычисления специальной формы @code{if} одинаково независимо от того,
+использует ли интерпретатор нормальный или аппликативный порядок: сначала
+вычисляется выражение-предикат, и результат определяет, вычислять ли
+выражение-следствие или выражение-альтернативу.)
 @end quotation
 
 @anchor{Sec.1.1.7}
