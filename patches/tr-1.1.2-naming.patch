diff --git a/sicp-pocket.texi b/sicp-pocket.texi
index 0b291e8..f97fe8a 100644
--- a/sicp-pocket.texi
+++ b/sicp-pocket.texi
@@ -1291,24 +1291,24 @@ Lisp поначалу был весьма неэффективен для чис
 всего, но не знают стоимости ничего».}
 
 @node	1.1.2, 1.1.3, 1.1.1, 1.1
-@subsection Naming and the Environment
+@subsection Именование и окружение
 
-A critical aspect of a programming language is the means it provides for using
-names to refer to computational objects.  We say that the name identifies a
-@newterm{variable} whose @newterm{value} is the object.
+Важнейший аспект языка программирования — предоставляемые им средства использовать
+имена для обращения к вычислительным объектам.  Мы говорим, что имя обозначает
+@newterm{переменную}, @newterm{значением} которой является объект.
 
-In the Scheme dialect of Lisp, we name things with @code{define}.  Typing
+В диалекте Lisp под названием Scheme мы именуем вещи с помощью @code{define}.
+Набрав
 
 @lisp
 (define size 2)
 @end lisp
 
 @noindent
-causes the interpreter to associate the value 2 with the name
-@code{size}.@footnote{In this book, we do not show the interpreter's response
-to evaluating definitions, since this is highly implementation-dependent.} Once
-the name @code{size} has been associated with the number 2, we can refer to the
-value 2 by name:
+мы заставляем интерпретатор связать значение 2 с именем
+@code{size}.@footnote{В этой книге мы не показываем ответ интерпретатора на
+вычисление определений, поскольку он сильно зависит от реализации.} Как только
+имя @code{size} связано с числом 2, мы можем обращаться к значению 2 по имени:
 
 @lisp
 size
@@ -1319,7 +1319,7 @@ size
 @end lisp
 
 @noindent
-Here are further examples of the use of @code{define}:
+Вот ещё примеры использования @code{define}:
 
 @lisp
 (define pi 3.14159)
@@ -1335,27 +1335,26 @@ circumference
 @end lisp
 
 @noindent
-@code{Define} is our language's simplest means of abstraction, for it allows us
-to use simple names to refer to the results of compound operations, such as the
-@code{circumference} computed above.  In general, computational objects may
-have very complex structures, and it would be extremely inconvenient to have to
-remember and repeat their details each time we want to use them.  Indeed,
-complex programs are constructed by building, step by step, computational
-objects of increasing complexity. The interpreter makes this step-by-step
-program construction particularly convenient because name-object associations
-can be created incrementally in successive interactions.  This feature
-encourages the incremental development and testing of programs and is largely
-responsible for the fact that a Lisp program usually consists of a large number
-of relatively simple procedures.
-
-It should be clear that the possibility of associating values with symbols and
-later retrieving them means that the interpreter must maintain some sort of
-memory that keeps track of the name-object pairs.  This memory is called the
-@newterm{environment} (more precisely the @newterm{global environment}, since
-we will see later that a computation may involve a number of different
-environments).@footnote{@ref{Chapter 3} will show that this notion of
-environment is crucial, both for understanding how the interpreter works and
-for implementing interpreters.}
+@code{Define} — простейшее средство абстракции в нашем языке, ведь оно позволяет
+нам использовать простые имена для обращения к результатам составных операций,
+таким как длина окружности @code{circumference}, вычисленная выше.  Вообще говоря,
+вычислительные объекты могут иметь весьма сложную структуру, и было бы крайне
+неудобно всякий раз, когда мы хотим ими воспользоваться, держать в памяти и
+повторять все их детали.  В самом деле, сложные программы строятся постепенным
+возведением вычислительных объектов всё возрастающей сложности.  Интерпретатор
+делает такое пошаговое построение программ особенно удобным, потому что связи
+«имя — объект» можно создавать постепенно, в последовательных взаимодействиях.
+Эта особенность поощряет постепенную разработку и тестирование программ и во
+многом ответственна за то, что программа на Lisp обычно состоит из большого числа
+сравнительно простых процедур.
+
+Должно быть ясно, что возможность связывать значения с символами и позже извлекать
+их означает, что интерпретатор должен поддерживать своего рода память,
+отслеживающую пары «имя — объект».  Эта память называется @newterm{окружением}
+(точнее, @newterm{глобальным окружением}, поскольку позже мы увидим, что
+вычисление может затрагивать несколько разных окружений).@footnote{@ref{Chapter 3}
+покажет, что это понятие окружения принципиально важно как для понимания работы
+интерпретатора, так и для реализации интерпретаторов.}
 
 @node	1.1.3, 1.1.4, 1.1.2, 1.1
 @subsection Evaluating Combinations
