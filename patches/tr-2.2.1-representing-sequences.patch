diff --git a/sicp-pocket.texi b/sicp-pocket.texi
index 4397aa0..fb6e376 100644
--- a/sicp-pocket.texi
+++ b/sicp-pocket.texi
@@ -6763,19 +6763,18 @@ R_1 + R_2
 @end menu
 
 @node	2.2.1, 2.2.2, 2.2, 2.2
-@subsection Representing Sequences
+@subsection Представление последовательностей
 
-One of the useful structures we can build with pairs is a
-@newterm{sequence}---an ordered collection of data objects.  There are, of
-course, many ways to represent sequences in terms of pairs.  One particularly
-straightforward representation is illustrated in @ref{Figure 2.4}, where the
-sequence 1, 2, 3, 4 is represented as a chain of pairs.  The @code{car} of each
-pair is the corresponding item in the chain, and the @code{cdr} of the pair is
-the next pair in the chain.  The @code{cdr} of the final pair signals the end
-of the sequence by pointing to a distinguished value that is not a pair,
-represented in box-and-pointer diagrams as a diagonal line and in programs as
-the value of the variable @code{nil}.  The entire sequence is constructed by
-nested @code{cons} operations:
+Одна из полезных структур, которые мы можем построить с помощью пар, — это
+@newterm{последовательность}, упорядоченный набор объектов данных.  Существует, конечно,
+много способов представления последовательностей в терминах пар.  Одно особенно
+прямолинейное представление проиллюстрировано на @ref{Figure 2.4}, где
+последовательность 1, 2, 3, 4 представлена как цепочка пар.  @code{Car} каждой пары —
+это соответствующий элемент цепочки, а @code{cdr} пары — следующая пара в цепочке.
+@code{Cdr} последней пары сигнализирует о конце последовательности, указывая на
+выделенное значение, не являющееся парой, которое представляется на диаграммах «коробки
+и указатели» диагональной линией, а в программах — значением переменной @code{nil}.
+Вся последовательность строится вложенными операциями @code{cons}:
 
 @lisp
 (cons 1
@@ -6788,7 +6787,7 @@ nested @code{cons} operations:
 @anchor{Figure 2.4}
 @ifinfo
 @quotation
-@strong{Figure 2.4:} The sequence 1, 2, 3, 4 represented as a chain of pairs.
+@strong{Figure 2.4:} Последовательность 1, 2, 3, 4, представленная как цепочка пар.
 
 @example
      +---+---+     +---+---+     +---+---+     +---+---+
@@ -6804,25 +6803,25 @@ nested @code{cons} operations:
 @end ifinfo
 @iftex
 @image{fig/chap2/Fig2.4e,125mm,,,.std.svg}
-@caption{@strong{Figure 2.4:} The sequence 1, 2, 3, 4 represented as a chain of pairs.}
+@caption{@strong{Figure 2.4:} Последовательность 1, 2, 3, 4, представленная как цепочка пар.}
 @end iftex
 @end float
 
 @noindent
-Such a sequence of pairs, formed by nested @code{cons}es, is called a
-@newterm{list}, and Scheme provides a primitive called @code{list} to help in
-constructing lists.@footnote{In this book, we use @newterm{list} to mean a
-chain of pairs terminated by the end-of-list marker.  In contrast, the term
-@newterm{list structure} refers to any data structure made out of pairs, not
-just to lists.}  The above sequence could be produced by @code{(list 1 2 3 4)}.
-In general,
+Такая последовательность пар, образованная вложенными @code{cons}, называется
+@newterm{списком}, и Scheme предоставляет примитив под названием @code{list}, помогающий
+строить списки.@footnote{В этой книге мы используем слово @newterm{список} для обозначения
+цепочки пар, оканчивающейся маркером конца списка.  В отличие от этого, термин
+@newterm{списочная структура} относится к любой структуре данных, составленной из пар, а
+не только к спискам.}  Приведённую выше последовательность можно было бы получить с
+помощью @code{(list 1 2 3 4)}.  Вообще,
 
 @lisp
 (list ⟨@var{a₁}⟩ ⟨@var{a₂}⟩ @r{…} ⟨@var{aₙ}⟩)
 @end lisp
 
 @noindent
-is equivalent to
+эквивалентно
 
 @lisp
 (cons ⟨@var{a₁}⟩
@@ -6833,9 +6832,9 @@ is equivalent to
 @end lisp
 
 @noindent
-Lisp systems conventionally print lists by printing the sequence of elements,
-enclosed in parentheses.  Thus, the data object in @ref{Figure 2.4} is printed
-as @code{(1 2 3 4)}:
+Lisp-системы по соглашению печатают списки, печатая последовательность элементов,
+заключённую в скобки.  Таким образом, объект данных на @ref{Figure 2.4} печатается как
+@code{(1 2 3 4)}:
 
 @lisp
 (define one-through-four (list 1 2 3 4))
@@ -6845,30 +6844,28 @@ one-through-four
 @end lisp
 
 @noindent
-Be careful not to confuse the expression @code{(list 1 2 3 4)} with the list
-@code{(1 2 3 4)}, which is the result obtained when the expression is
-evaluated.  Attempting to evaluate the expression @code{(1 2 3 4)} will signal
-an error when the interpreter tries to apply the procedure @code{1} to
-arguments @code{2}, @code{3}, @code{4}.
+Будьте внимательны, чтобы не спутать выражение @code{(list 1 2 3 4)} со списком
+@code{(1 2 3 4)}, который является результатом, получаемым при вычислении выражения.
+Попытка вычислить выражение @code{(1 2 3 4)} вызовет ошибку, когда интерпретатор
+попытается применить процедуру @code{1} к аргументам @code{2}, @code{3}, @code{4}.
 
-We can think of @code{car} as selecting the first item in the list, and of
-@code{cdr} as selecting the sublist consisting of all but the first item.
-Nested applications of @code{car} and @code{cdr} can be used to extract the
-second, third, and subsequent items in the list.@footnote{Since nested
-applications of @code{car} and @code{cdr} are cumbersome to write, Lisp
-dialects provide abbreviations for them---for instance,
+Мы можем мыслить @code{car} как выбор первого элемента списка, а @code{cdr} — как
+выбор подсписка, состоящего из всех элементов, кроме первого.  Вложенные применения
+@code{car} и @code{cdr} можно использовать для извлечения второго, третьего и
+последующих элементов списка.@footnote{Поскольку вложенные применения @code{car} и
+@code{cdr} громоздко записывать, диалекты Lisp предоставляют для них сокращения —
+например,
 
 @lisp
-(cadr ⟨@var{arg}⟩) = (car (cdr ⟨@var{arg}⟩))
+(cadr ⟨@var{арг}⟩) = (car (cdr ⟨@var{арг}⟩))
 @end lisp
 
-The names of all such procedures start with @code{c} and end with @code{r}.
-Each @code{a} between them stands for a @code{car} operation and each @code{d}
-for a @code{cdr} operation, to be applied in the same order in which they
-appear in the name.  The names @code{car} and @code{cdr} persist because simple
-combinations like @code{cadr} are pronounceable.} The constructor @code{cons}
-makes a list like the original one, but with an additional item at the
-beginning.
+Имена всех таких процедур начинаются с @code{c} и оканчиваются на @code{r}.  Каждая
+@code{a} между ними обозначает операцию @code{car}, а каждая @code{d} — операцию
+@code{cdr}, применяемые в том же порядке, в каком они появляются в имени.  Имена
+@code{car} и @code{cdr} сохраняются, потому что простые комбинации вроде @code{cadr}
+произносимы.} Конструктор @code{cons} делает список, подобный исходному, но с
+дополнительным элементом в начале.
 
 @lisp
 (car one-through-four)
@@ -6888,38 +6885,37 @@ beginning.
 @end lisp
 
 @noindent
-The value of @code{nil}, used to terminate the chain of pairs, can be thought
-of as a sequence of no elements, the @newterm{empty list}.  The word
-@newterm{nil} is a contraction of the Latin word @emph{nihil}, which means
-``nothing.''@footnote{It's remarkable how much energy in the standardization of
-Lisp dialects has been dissipated in arguments that are literally over nothing:
-Should @code{nil} be an ordinary name?  Should the value of @code{nil} be a
-symbol?  Should it be a list?  Should it be a pair?  In Scheme, @code{nil} is
-an ordinary name, which we use in this section as a variable whose value is the
-end-of-list marker (just as @code{true} is an ordinary variable that has a true
-value).  Other dialects of Lisp, including Common Lisp, treat @code{nil} as a
-special symbol.  The authors of this book, who have endured too many language
-standardization brawls, would like to avoid the entire issue.  Once we have
-introduced quotation in @ref{2.3}, we will denote the empty list as
-@code{'()} and dispense with the variable @code{nil} entirely.}
+Значение @code{nil}, используемое для завершения цепочки пар, можно мыслить как
+последовательность из нуля элементов — @newterm{пустой список}.  Слово @newterm{nil} —
+это сокращение латинского слова @emph{nihil}, которое означает
+«ничто».@footnote{Поразительно, сколько энергии при стандартизации диалектов Lisp было
+растрачено на споры, которые буквально идут ни о чём: должен ли @code{nil} быть
+обычным именем?  Должно ли значение @code{nil} быть символом?  Должно ли оно быть
+списком?  Должно ли оно быть парой?  В Scheme @code{nil} — обычное имя, которое мы
+используем в этом разделе как переменную, значением которой является маркер конца
+списка (точно так же, как @code{true} — обычная переменная, имеющая истинное значение).
+Другие диалекты Lisp, включая Common Lisp, трактуют @code{nil} как особый символ.
+Авторы этой книги, претерпевшие слишком много потасовок при стандартизации языков,
+хотели бы вообще избежать этого вопроса.  Как только мы введём цитирование в @ref{2.3},
+мы будем обозначать пустой список как @code{'()} и совсем откажемся от переменной
+@code{nil}.}
 
-@subsubheading List operations
+@subsubheading Операции над списками
 
-The use of pairs to represent sequences of elements as lists is accompanied by
-conventional programming techniques for manipulating lists by successively
-``@code{cdr}ing down'' the lists.  For example, the procedure @code{list-ref}
-takes as arguments a list and a number @math{n} and returns the @math{n^{\text{th}}} item of
-the list.  It is customary to number the elements of the list beginning with 0.
-The method for computing @code{list-ref} is the following:
+Использование пар для представления последовательностей элементов в виде списков
+сопровождается традиционными приёмами программирования для манипулирования списками
+посредством последовательного «спуска по списку через @code{cdr}».  Например,
+процедура @code{list-ref} принимает в качестве аргументов список и число @math{n} и
+возвращает @math{n}-й элемент списка.  Принято нумеровать элементы списка начиная с 0.
+Метод вычисления @code{list-ref} следующий:
 
 @itemize @bullet
 
 @item
-For @math{{n = 0}}, @code{list-ref} should return the @code{car} of the list.
+Для @math{{n = 0}} @code{list-ref} должна вернуть @code{car} списка.
 
 @item
-Otherwise, @code{list-ref} should return  the @math{{(n - 1)}}-st item of the
-@code{cdr} of the list.
+Иначе @code{list-ref} должна вернуть @math{{(n - 1)}}-й элемент @code{cdr} списка.
 
 @end itemize
 
@@ -6938,10 +6934,10 @@ Otherwise, @code{list-ref} should return  the @math{{(n - 1)}}-st item of the
 @end lisp
 
 @noindent
-Often we @code{cdr} down the whole list.  To aid in this, Scheme includes a
-primitive predicate @code{null?}, which tests whether its argument is the empty
-list.  The procedure @code{length}, which returns the number of items in a
-list, illustrates this typical pattern of use:
+Часто мы спускаемся по всему списку через @code{cdr}.  Чтобы помочь в этом, Scheme
+включает примитивный предикат @code{null?}, который проверяет, является ли его аргумент
+пустым списком.  Процедура @code{length}, возвращающая число элементов в списке,
+иллюстрирует этот типичный образец использования:
 
 @lisp
 (define (length items)
@@ -6957,29 +6953,27 @@ list, illustrates this typical pattern of use:
 @end lisp
 
 @noindent
-The @code{length} procedure implements a simple recursive plan. The reduction
-step is:
+Процедура @code{length} реализует простой рекурсивный план.  Шаг сведения таков:
 
 @itemize @bullet
 
 @item
-The @code{length} of any list is 1 plus the @code{length} of the @code{cdr} of
-the list.
+@code{Length} любого списка — это 1 плюс @code{length} от @code{cdr} списка.
 
 @end itemize
 
 @noindent
-This is applied successively until we reach the base case:
+Это применяется последовательно, пока мы не достигнем базового случая:
 
 @itemize @bullet
 
 @item
-The @code{length} of the empty list is 0.
+@code{Length} пустого списка равна 0.
 
 @end itemize
 
 @noindent
-We could also compute @code{length} in an iterative style:
+Мы могли бы также вычислять @code{length} в итеративном стиле:
 
 @lisp
 (define (length items)
@@ -6992,9 +6986,10 @@ We could also compute @code{length} in an iterative style:
 @end lisp
 
 @noindent
-Another conventional programming technique is to ``@code{cons} up'' an answer
-list while @code{cdr}ing down a list, as in the procedure @code{append}, which
-takes two lists as arguments and combines their elements to make a new list:
+Другой традиционный приём программирования — «наращивать через @code{cons}»
+список-ответ, спускаясь по списку через @code{cdr}, как в процедуре @code{append},
+которая принимает два списка как аргументы и комбинирует их элементы, образуя новый
+список:
 
 @lisp
 (append squares odds)
@@ -7005,17 +7000,17 @@ takes two lists as arguments and combines their elements to make a new list:
 @end lisp
 
 @noindent
-@code{Append} is also implemented using a recursive plan.  To @code{append}
-lists @code{list1} and @code{list2}, do the following:
+@code{Append} тоже реализуется с помощью рекурсивного плана.  Чтобы выполнить
+@code{append} над списками @code{list1} и @code{list2}, сделайте следующее:
 
 @itemize @bullet
 
 @item
-If @code{list1} is the empty list, then the result is just @code{list2}.
+Если @code{list1} — пустой список, то результат — просто @code{list2}.
 
 @item
-Otherwise, @code{append} the @code{cdr} of @code{list1} and @code{list2}, and
-@code{cons} the @code{car} of @code{list1} onto the result:
+Иначе выполните @code{append} над @code{cdr} списка @code{list1} и @code{list2} и
+прицепите через @code{cons} @code{car} списка @code{list1} к результату:
 
 @end itemize
 
@@ -7029,9 +7024,9 @@ Otherwise, @code{append} the @code{cdr} of @code{list1} and @code{list2}, and
 @end lisp
 
 @quotation
-@strong{@anchor{Exercise 2.17}Exercise 2.17:} Define a procedure
-@code{last-pair} that returns the list that contains only the last element of a
-given (nonempty) list:
+@strong{@anchor{Exercise 2.17}Exercise 2.17:} Определите процедуру @code{last-pair},
+которая возвращает список, содержащий только последний элемент данного (непустого)
+списка:
 
 @lisp
 (last-pair (list 23 72 149 34))
@@ -7040,9 +7035,9 @@ given (nonempty) list:
 @end quotation
 
 @quotation
-@strong{@anchor{Exercise 2.18}Exercise 2.18:} Define a procedure @code{reverse}
-that takes a list as argument and returns a list of the same elements in
-reverse order:
+@strong{@anchor{Exercise 2.18}Exercise 2.18:} Определите процедуру @code{reverse},
+которая принимает список как аргумент и возвращает список из тех же элементов в
+обратном порядке:
 
 @lisp
 (reverse (list 1 4 9 16 25))
@@ -7051,18 +7046,17 @@ reverse order:
 @end quotation
 
 @quotation
-@strong{@anchor{Exercise 2.19}Exercise 2.19:} Consider the change-counting
-program of @ref{1.2.2}.  It would be nice to be able to easily change
-the currency used by the program, so that we could compute the number of ways
-to change a British pound, for example.  As the program is written, the
-knowledge of the currency is distributed partly into the procedure
-@code{first-denomination} and partly into the procedure @code{count-change}
-(which knows that there are five kinds of U.S. coins).  It would be nicer to be
-able to supply a list of coins to be used for making change.
+@strong{@anchor{Exercise 2.19}Exercise 2.19:} Рассмотрите программу размена монет из
+@ref{1.2.2}.  Было бы хорошо иметь возможность легко менять используемую программой
+валюту, чтобы мы могли вычислять, например, число способов разменять британский фунт.
+В том виде, в каком программа написана, знание о валюте распределено частично в
+процедуре @code{first-denomination}, а частично — в процедуре @code{count-change}
+(которая знает, что есть пять видов монет США).  Было бы приятнее иметь возможность
+снабжать программу списком монет, используемых для размена.
 
-We want to rewrite the procedure @code{cc} so that its second argument is a
-list of the values of the coins to use rather than an integer specifying which
-coins to use.  We could then have lists that defined each kind of currency:
+Мы хотим переписать процедуру @code{cc} так, чтобы её второй аргумент был списком
+номиналов используемых монет, а не целым числом, указывающим, какие монеты
+использовать.  Тогда мы могли бы иметь списки, определяющие каждый вид валюты:
 
 @lisp
 (define us-coins 
@@ -7072,16 +7066,16 @@ coins to use.  We could then have lists that defined each kind of currency:
   (list 100 50 20 10 5 2 1 0.5))
 @end lisp
 
-We could then call @code{cc} as follows:
+Тогда мы могли бы вызывать @code{cc} так:
 
 @lisp
 (cc 100 us-coins)
 @i{292}
 @end lisp
 
-To do this will require changing the program @code{cc} somewhat.  It will still
-have the same form, but it will access its second argument differently, as
-follows:
+Чтобы сделать это, потребуется несколько изменить программу @code{cc}.  Она по-прежнему
+будет иметь ту же форму, но будет обращаться к своему второму аргументу иначе, а
+именно:
 
 @lisp
 (define (cc amount coin-values)
@@ -7102,64 +7096,61 @@ follows:
              coin-values)))))
 @end lisp
 
-Define the procedures @code{first-denomination},
-@code{except-first-denomination} and @code{no-more?} in terms of primitive
-operations on list structures.  Does the order of the list @code{coin-values}
-affect the answer produced by @code{cc}?  Why or why not?
+Определите процедуры @code{first-denomination}, @code{except-first-denomination} и
+@code{no-more?} в терминах примитивных операций над списочными структурами.  Влияет ли
+порядок списка @code{coin-values} на ответ, выдаваемый @code{cc}?  Почему да или почему
+нет?
 @end quotation
 
 @quotation
-@strong{@anchor{Exercise 2.20}Exercise 2.20:} The procedures @code{+},
-@code{*}, and @code{list} take arbitrary numbers of arguments. One way to
-define such procedures is to use @code{define} with @newterm{dotted-tail notation}.  
-In a procedure definition, a parameter list that has a dot before
-the last parameter name indicates that, when the procedure is called, the
-initial parameters (if any) will have as values the initial arguments, as
-usual, but the final parameter's value will be a @newterm{list} of any
-remaining arguments.  For instance, given the definition
+@strong{@anchor{Exercise 2.20}Exercise 2.20:} Процедуры @code{+}, @code{*} и
+@code{list} принимают произвольное число аргументов.  Один способ определять такие
+процедуры — использовать @code{define} с @newterm{точечно-хвостовой нотацией}.  В
+определении процедуры список параметров, в котором есть точка перед именем последнего
+параметра, указывает, что при вызове процедуры начальные параметры (если они есть) будут
+иметь своими значениями начальные аргументы, как обычно, а значением последнего
+параметра будет @newterm{список} любых оставшихся аргументов.  Например, при определении
 
 @lisp
-(define (f x y . z) ⟨@var{body}⟩)
+(define (f x y . z) ⟨@var{тело}⟩)
 @end lisp
 
 @noindent
-the procedure @code{f} can be called with two or more arguments.  If we
-evaluate
+процедуру @code{f} можно вызывать с двумя или более аргументами.  Если мы вычислим
 
 @lisp
 (f 1 2 3 4 5 6)
 @end lisp
 
 @noindent
-then in the body of @code{f}, @code{x} will be 1, @code{y} will be 2, and
-@code{z} will be the list @code{(3 4 5 6)}.  Given the definition
+то в теле @code{f} @code{x} будет 1, @code{y} будет 2, а @code{z} будет списком
+@code{(3 4 5 6)}.  При определении
 
 @lisp
-(define (g . w) ⟨@var{body}⟩)
+(define (g . w) ⟨@var{тело}⟩)
 @end lisp
 
 @noindent
-the procedure @code{g} can be called with zero or more arguments.  If we
-evaluate
+процедуру @code{g} можно вызывать с нулём или более аргументов.  Если мы вычислим
 
 @lisp
 (g 1 2 3 4 5 6)
 @end lisp
 
 @noindent
-then in the body of @code{g}, @code{w} will be the list @code{(1 2 3 4 5
-6)}.@footnote{To define @code{f} and @code{g} using @code{lambda} we would
-write
+то в теле @code{g} @code{w} будет списком @code{(1 2 3 4 5
+6)}.@footnote{Чтобы определить @code{f} и @code{g} с помощью @code{lambda}, мы написали
+бы
 
 @lisp
-(define f (lambda (x y . z) ⟨@var{body}⟩))
-(define g (lambda w ⟨@var{body}⟩))
+(define f (lambda (x y . z) ⟨@var{тело}⟩))
+(define g (lambda w ⟨@var{тело}⟩))
 @end lisp
 }
 
-Use this notation to write a procedure @code{same-parity} that takes one or
-more integers and returns a list of all the arguments that have the same
-even-odd parity as the first argument.  For example,
+Воспользуйтесь этой нотацией, чтобы написать процедуру @code{same-parity}, которая
+принимает одно или более целых чисел и возвращает список всех аргументов, имеющих ту же
+чётность, что и первый аргумент.  Например,
 
 @lisp
 (same-parity 1 2 3 4 5 6 7)
@@ -7170,11 +7161,11 @@ even-odd parity as the first argument.  For example,
 @end lisp
 @end quotation
 
-@subsubheading Mapping over lists
+@subsubheading Отображение списков
 
-One extremely useful operation is to apply some transformation to each element
-in a list and generate the list of results.  For instance, the following
-procedure scales each number in a list by a given factor:
+Одна чрезвычайно полезная операция — применить некоторое преобразование к каждому
+элементу списка и породить список результатов.  Например, следующая процедура
+масштабирует каждое число в списке на заданный множитель:
 
 @lisp
 (define (scale-list items factor)
@@ -7189,16 +7180,16 @@ procedure scales each number in a list by a given factor:
 @end lisp
 
 @noindent
-We can abstract this general idea and capture it as a common pattern expressed
-as a higher-order procedure, just as in @ref{1.3}.  The higher-order
-procedure here is called @code{map}.  @code{Map} takes as arguments a procedure
-of one argument and a list, and returns a list of the results produced by
-applying the procedure to each element in the list:@footnote{@anchor{Footnote 78}
-Scheme standardly provides a @code{map} procedure that
-is more general than the one described here.  This more general @code{map}
-takes a procedure of @math{n} arguments, together with @math{n} lists, and applies
-the procedure to all the first elements of the lists, all the second elements
-of the lists, and so on, returning a list of the results.  For example:
+Мы можем абстрагировать эту обобщённую идею и схватить её как общий образец, выраженный
+процедурой высшего порядка, точно так же, как в @ref{1.3}.  Процедура высшего порядка
+здесь называется @code{map}.  @code{Map} принимает в качестве аргументов процедуру
+одного аргумента и список и возвращает список результатов, полученных применением
+процедуры к каждому элементу списка:@footnote{@anchor{Footnote 78}
+Scheme стандартно предоставляет процедуру @code{map}, которая более обобщённа, чем
+описанная здесь.  Этот более обобщённый @code{map} принимает процедуру @math{n}
+аргументов вместе с @math{n} списками и применяет процедуру ко всем первым элементам
+списков, всем вторым элементам списков и так далее, возвращая список результатов.
+Например:
 
 @lisp
 (map + 
@@ -7229,7 +7220,7 @@ of the lists, and so on, returning a list of the results.  For example:
 @end lisp
 
 @noindent
-Now we can give a new definition of @code{scale-list} in terms of @code{map}:
+Теперь мы можем дать новое определение @code{scale-list} в терминах @code{map}:
 
 @lisp
 (define (scale-list items factor)
@@ -7238,35 +7229,32 @@ Now we can give a new definition of @code{scale-list} in terms of @code{map}:
 @end lisp
 
 @noindent
-@code{Map} is an important construct, not only because it captures a common
-pattern, but because it establishes a higher level of abstraction in dealing
-with lists.  In the original definition of @code{scale-list}, the recursive
-structure of the program draws attention to the element-by-element processing
-of the list.  Defining @code{scale-list} in terms of @code{map} suppresses that
-level of detail and emphasizes that scaling transforms a list of elements to a
-list of results.  The difference between the two definitions is not that the
-computer is performing a different process (it isn't) but that we think about
-the process differently.  In effect, @code{map} helps establish an abstraction
-barrier that isolates the implementation of procedures that transform lists
-from the details of how the elements of the list are extracted and combined.
-Like the barriers shown in @ref{Figure 2.1}, this abstraction gives us the
-flexibility to change the low-level details of how sequences are implemented,
-while preserving the conceptual framework of operations that transform
-sequences to sequences.  Section @ref{2.2.3} expands on this use of sequences
-as a framework for organizing programs.
+@code{Map} — важная конструкция не только потому, что она схватывает общий образец, но
+и потому, что она устанавливает более высокий уровень абстракции в работе со списками.
+В исходном определении @code{scale-list} рекурсивная структура программы привлекает
+внимание к поэлементной обработке списка.  Определение @code{scale-list} в терминах
+@code{map} подавляет этот уровень подробностей и подчёркивает, что масштабирование
+преобразует список элементов в список результатов.  Различие между двумя определениями
+не в том, что компьютер выполняет другой процесс (это не так), а в том, что мы думаем о
+процессе по-другому.  По сути, @code{map} помогает установить барьер абстракции,
+изолирующий реализацию процедур, преобразующих списки, от подробностей того, как
+элементы списка извлекаются и комбинируются.  Подобно барьерам, показанным на
+@ref{Figure 2.1}, эта абстракция даёт нам гибкость менять низкоуровневые подробности
+того, как реализованы последовательности, сохраняя при этом концептуальный каркас
+операций, преобразующих последовательности в последовательности.  Раздел @ref{2.2.3}
+развивает это использование последовательностей как каркаса для организации программ.
 
 @quotation
-@strong{@anchor{Exercise 2.21}Exercise 2.21:} The procedure @code{square-list}
-takes a list of numbers as argument and returns a list of the squares of those
-numbers.
+@strong{@anchor{Exercise 2.21}Exercise 2.21:} Процедура @code{square-list} принимает
+список чисел как аргумент и возвращает список квадратов этих чисел.
 
 @lisp
 (square-list (list 1 2 3 4))
 @i{(1 4 9 16)}
 @end lisp
 
-Here are two different definitions of @code{square-list}.  Complete both of
-them by filling in the missing expressions:
+Вот два различных определения @code{square-list}.  Дополните оба, заполнив недостающие
+выражения:
 
 @lisp
 (define (square-list items)
@@ -7280,9 +7268,9 @@ them by filling in the missing expressions:
 @end quotation
 
 @quotation
-@strong{@anchor{Exercise 2.22}Exercise 2.22:} Louis Reasoner tries to rewrite
-the first @code{square-list} procedure of @ref{Exercise 2.21} so that it
-evolves an iterative process:
+@strong{@anchor{Exercise 2.22}Exercise 2.22:} Луи Резонер пытается переписать первую
+процедуру @code{square-list} из @ref{Exercise 2.21} так, чтобы она развивала
+итеративный процесс:
 
 @lisp
 (define (square-list items)
@@ -7295,10 +7283,10 @@ evolves an iterative process:
   (iter items nil))
 @end lisp
 
-Unfortunately, defining @code{square-list} this way produces the answer list in
-the reverse order of the one desired.  Why?
+К сожалению, определённая таким образом @code{square-list} выдаёт список-ответ в
+порядке, обратном желаемому.  Почему?
 
-Louis then tries to fix his bug by interchanging the arguments to @code{cons}:
+Затем Луи пытается исправить свою ошибку, переставив аргументы @code{cons}:
 
 @lisp
 (define (square-list items)
@@ -7312,17 +7300,16 @@ Louis then tries to fix his bug by interchanging the arguments to @code{cons}:
   (iter items nil))
 @end lisp
 
-This doesn't work either.  Explain.
+Это тоже не работает.  Объясните.
 @end quotation
 
 @quotation
-@strong{@anchor{Exercise 2.23}Exercise 2.23:} The procedure @code{for-each} is
-similar to @code{map}.  It takes as arguments a procedure and a list of
-elements.  However, rather than forming a list of the results, @code{for-each}
-just applies the procedure to each of the elements in turn, from left to right.
-The values returned by applying the procedure to the elements are not used at
-all---@code{for-each} is used with procedures that perform an action, such as
-printing.  For example,
+@strong{@anchor{Exercise 2.23}Exercise 2.23:} Процедура @code{for-each} похожа на
+@code{map}.  Она принимает в качестве аргументов процедуру и список элементов.  Однако
+вместо того, чтобы образовывать список результатов, @code{for-each} просто применяет
+процедуру к каждому из элементов по очереди, слева направо.  Значения, возвращаемые
+применением процедуры к элементам, вообще не используются — @code{for-each} применяется
+с процедурами, выполняющими действие, такое как печать.  Например,
 
 @lisp
 (for-each 
@@ -7334,9 +7321,8 @@ printing.  For example,
 @i{88}
 @end lisp
 
-The value returned by the call to @code{for-each} (not illustrated above) can
-be something arbitrary, such as true.  Give an implementation of
-@code{for-each}.
+Значение, возвращаемое вызовом @code{for-each} (не показанное выше), может быть
+чем-то произвольным, например истиной.  Дайте реализацию @code{for-each}.
 @end quotation
 
 @node	2.2.2, 2.2.3, 2.2.1, 2.2
